# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*tfplan*

# Node (api local installs — CI builds its own via Oryx)
node_modules/
package-lock.json

# Playwright
playwright-report/
test-results/

# OS junk
.DS_Store
Thumbs.db
